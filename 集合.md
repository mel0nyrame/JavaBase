# 集合

- 数组与集合区别
  - 数组固定大小,集合大小可变
  - 数组能存放基本数据,集合只能存放对象

几乎所有集合类都实现了Collection接口,这个接口定义了集合类所拥有的方法

Collection接口里包含了增删改查已经交并补集的操作

## List集合(列表)

- 特点
  - 有序类,从头插到脚,有下标
  - 可以包含重复元素
  - 继承与Collection接口
  - 可直接获取下标,查询某个元素位置

### ArrayList

和数据结构中相同

### LinkedList

和数据结构中相同,不过是双向链表

|列表|特点|
|:--:|:--:|
|ArrayList|更适合随机访问,可以直接获取下标,不适合大量的插入删除|
|LinkedList|不适合随机访问,需要遍历得到元素,适合插入删除|

### Iterator(迭代器)

用于遍历集合中的元素

- 方法
  - boolean hasNext():是否有下一个元素
  - E next():输出下一个元素

>不同集合的实现方式不同

### ListIterator(针对list的迭代器)

- 方法
  - 继承迭代器的方法
  - 从后往前遍历
  - 获取索引
  - 增删改查元素

## Deque和Qeque(队列)

在Java底层均使用LinkedList实现

可以使用Queue<> queue = new LinkedList<>();来实现队列操作

- 方法
  - Queue(单项队列)
    - 在此接口中均有两个方法,一个会报错一个不会报错
  - Dueue(双端队列)
    - 既可以在队尾入列出列也可以在对首入列出列,可当作栈使用(也封装了栈类似的方法)
  - PriorityQueue(优先队列)
    - 可以保证根据次序出列(存放不一定)
